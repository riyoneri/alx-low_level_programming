#include <unistd.h>

/**
 * mul - print product of numbers
 * @a: First number to calculate product for
 * @b: Second number to calculate product for
 * Return: product
 */

int mul(int a, int b)
{
	return (a * b);
}
